import 'package:flutter/material.dart';

class campusvendor extends StatelessWidget {
  const campusvendor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}